framework: jest
preRun:
     command:
        - yarn install
preMerge:
  pattern:
    - "./test/e2e/*.test.js"
postMerge:
  pattern:
    - "./test/e2e/*.test.js"
postRun:
     command:
        - yarn --version
version: 1.0

import React, { PropTypes } from 'react';
import Helmet from 'react-helmet';
const About = (props) => {
  return (
    <div>
      <Helmet
        title="About"
        />
      <h2>About Page</h2>
    </div>
  );
};

export default About;

// Available theme variables can be found in
// https://github.com/ant-design/ant-design/blob/master/components/style/themes/default.less

// IMPORTANT: to see changes made to this file, a restart of the server is required.

// NOTE: Variables can also start with @ (like in less), but you will need it with the @ sign to call the variable/

module.exports = {
  'primary-color': '#ec6a00',
  // '@normal-color': '#d9d9d9', // Using @ sign in the variable name
  // 'layout-body-background': '#fff',
  // 'layout-header-background': '@layout-body-background', // Calling a variable (requires the @ sign)
  '@layout-header-padding': '0 1rem',
}
